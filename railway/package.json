{
  "name": "anjayslebew-railway",
  "version": "1.0.0",
  "private": true,
  "description": "Bandar80 claim engine — server selalu-hidup (Railway): API /api/* (worker 3 fase) + dashboard statis + opsi Playwright klik Approve/Reject.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "optionalDependencies": {
    "playwright": "^1.47.0"
  }
}