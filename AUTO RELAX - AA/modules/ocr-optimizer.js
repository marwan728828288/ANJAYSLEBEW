/* DEAD CODE — file ini tidak di-import/dimuat oleh mana pun (verifikasi: tidak
   ada referensi ke ocr-optimizer, OCRWorkerPool, getOptimalOCRVariants, dll di
   seluruh codebase & manifest). Isi lama dihapus. Silakan hapus file ini.
   Catatan: terminal tidak tersedia di sesi ini (Bash tidak terpasang), jadi
   file di-overwrite dengan placeholder ini, bukan di-delete. */
